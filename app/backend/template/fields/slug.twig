<div class="form-group {{ field.fieldsize ? field.fieldsize : '' }}">
    <label class=" form-control-label">{{ field.label ? field.label : property }}<small>{{ field.hint }}</small></label>

    <div class="input-group">
        <div class="input-group-prepend">
            <span class="input-group-text" id="basic-addon1"><i class="fa fa-link"></i></span>
        </div>
    <input type="text" placeholder="{{ field.placeholder ? field.placeholder : 'Inserisci valore' }}" class="form-control form-control-sm  "  slug-from="{{ field.templateVar }}" name="{{ property }}" value="{{ field.value }}">
    </div>
</div>